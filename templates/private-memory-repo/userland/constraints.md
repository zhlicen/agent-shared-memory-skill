# Constraints

Long-term constraints agents should respect.

## Context budget

- Add rules about when not to load more context.
- Example (replace): load at most two memory files per task.

## Privacy and sensitivity

- Never store secrets or credentials.
- Store personally identifying details (health, finances, employer, contacts, identifying paths) only with explicit user confirmation.
- Add anything else that should never be stored.

## Quality bar

- Add what makes a memory item worth keeping.
